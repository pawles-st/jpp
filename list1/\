#include "iter_module.h"

unsigned long factorial(unsigned int n) {
	if (n == 0) {
		return 1;
	} else {
		unsigned long result;
		while (n > 2) {
			n -= 1;
			result *= n;
		}
	}
	return result;
}

int main() {
	return 0;
}
